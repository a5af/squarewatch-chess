# Ignore all secrets and credentials in scripts directory
*.secret.*
*secret*
*password*
*credential*
*.key
*.pem
*.p12
*.pfx
.env
.env.*
!.env.example

# Ignore API keys and tokens
*api-key*
*api_key*
*token*
*access-key*

# Ignore temporary files
*.tmp
*.log
*.cache

# Keep these files (explicitly track)
!.gitignore
!README.md
!*.example.*
